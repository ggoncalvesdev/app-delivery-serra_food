import { StyleSheet, Platform } from "react-native";

export const styles = StyleSheet.create({
    title: {
        marginHorizontal: 30,
        marginVertical: 60,
        fontSize: 24,
    },
});
